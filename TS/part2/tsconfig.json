/*
 * tsconfig.json 是 ts 编译器的配置文件, ts 编译器可以根据它的信息来对代码进行编译
 * "include" 用来指定哪些 ts 文件需要被编译
 * "exclude" 不需要被编译的文件目录; 默认值: ["node_module", "bower_components", "jspm_packages"]
 */
{
  "include": [
    "./src/**/*" // ** 任意目录; * 任意文件
  ],
  "compilerOptions": {
    "module": "commonjs",
    "target": "es5",
    "sourceMap": true
  },
//  "exclude": [
//    "node_modules"
//  ]
}